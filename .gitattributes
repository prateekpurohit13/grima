# Normalize line endings. Shell scripts and Go sources must be LF even when
# checked out on Windows: a CRLF shebang fails on the Linux runner with
# "bad interpreter".
* text=auto eol=lf

# Binary assets are left untouched.
*.png binary
*.jpg binary
*.webp binary
*.ico binary
*.exe binary
*.dll binary
